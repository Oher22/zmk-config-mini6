#include <behaviors.dtsi>
#include <dt-bindings/zmk/keys.h>
#include <dt-bindings/zmk/bt.h>

/ {
    keymap {
        compatible = "zmk,keymap";

        default_layer {
            bindings = <
                &kp Q    &kp W    &mo 1
                &kp A    &kp S    &kp D
            >;
        };

        bt_layer {
            bindings = <
                &bootloader     &kp E   &none
                &kp SHIFT     &kp F        &kp SPACE
        };
    };
};
